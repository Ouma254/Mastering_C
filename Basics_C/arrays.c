#include <stdio.h>

// the main function
int main()
{
    // declare the variables
    float weight;
    int age;

    // take some input from the user
    puts("Enter some data");
    scanf("%d%f", &age, &weight);

    printf("The user age is %d", age);
}