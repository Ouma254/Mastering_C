#include <stdio.h>

int main(void) 
{
    // declare the variables
    signed int numbers[10];
    //unsigned float decimals[5];

    int num = 0;
    num++;
    printf("Number is %d numbers\n", num);
    printf("Size of %d bytes", sizeof(num));
}