#include <stdio.h>

// the main 
int main()
{
    // Array- are structures that hold data type of the same type
    // declare arrays
    int ages[10]; 

    // using the array indexes
    ages[1] = 19;
    ages[4] = 72;

    ages[0] = 56;
    
}