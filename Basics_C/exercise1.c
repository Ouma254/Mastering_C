#include <stdio.h>

// the main method
void main(void) {
    /*
    declare the variables
    */
    char description[50];

    // allow user to enter
    puts("Enter the name: ");
    scanf("%s", description);

    printf("Username %s", description);
    
    // no return 
}